function toggleLed(toggle, gundam_name) {
    switch (toggle) {
        case "on":
            ledOn(gundam_name);
            break;
        case "off":
            ledOff(gundam_name);
            break;
    }
}

function ledOn(gundam_name) {

}

function ledOff(gundam_name) {

}